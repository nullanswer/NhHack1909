<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nhhack1909">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".CompleteContractActivity"></activity>
        <activity
            android:name=".Contract1Activity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".ReqActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".DetailInfoActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".SearchResultActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".SearchActivity"
            android:theme="@style/MainTheme" />

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="brzf1doney" />

        <activity
            android:name=".MapActivity"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/LoginTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntroActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/MainTheme" />
        <activity android:name=".WebViewMapActivity" />
    </application>

</manifest>